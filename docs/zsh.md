# ZSH Config 

Set zsh as main shell: 
```shell 
sudo pacman -S zsh

chsh svartorm

reboot
```

Install shell utils:
```shell
sudo pacman -S zoxide eza fzf
paru -S oh-my-posh catnap
```

Other useful apps:
```shell
sudo pacman -S lazygit less
```
